name: connect_ab

description: Connect AB social communication app

publish_to: "none"

version: 1.0.0+1


environment:
  sdk: ">=3.0.0 <4.0.0"


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^3.15.0
  firebase_auth: ^5.7.0
  firebase_storage: ^12.4.7  
  firebase_messaging: ^15.2.7
  cloud_firestore: ^5.6.12
  image_picker: ^1.1.2
  video_player: ^2.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:

  uses-material-design: true
